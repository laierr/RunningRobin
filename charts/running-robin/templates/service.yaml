apiVersion: v1
kind: Service
metadata:
  name: {{ include "running-robin.fullname" . }}
  labels:
    {{- include "running-robin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "running-robin.selectorLabels" . | nindent 4 }}
